CREATE TABLE IF NOT EXISTS "CashAdjustment" ("id" TEXT NOT NULL, "amount" DOUBLE PRECISION NOT NULL, "reason" TEXT NOT NULL, "notes" TEXT NOT NULL, "userId" TEXT NOT NULL, "registerId" TEXT, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "CashAdjustment_pkey" PRIMARY KEY ("id"));
